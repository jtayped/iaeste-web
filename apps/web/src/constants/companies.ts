import { ChartSpline, Lightbulb, LucideIcon, UserPlus } from "lucide-react";

export const reasonIcons: Record<string, LucideIcon> = {
  talent: UserPlus,
  innovation: Lightbulb,
  growth: ChartSpline,
};
